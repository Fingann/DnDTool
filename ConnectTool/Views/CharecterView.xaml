<UserControl
    x:Class="DnDTool.Views.CharecterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Custom="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:charModel="clr-namespace:DnDTool.Model.Character"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:DnDTool.Views.CharecterViews"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="900">
    <UserControl.Resources>
        <Thickness x:Key="InfoMargin">0,5,0,5</Thickness>
        <DataTemplate DataType="{x:Type charModel:Info}">
            <views:CharecterInfoView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type charModel:AbilityScores}">
            <views:AbilityScoresView />
        </DataTemplate>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Path="Caller" Source="{StaticResource Locator}" />
    </UserControl.DataContext>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"  />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="532" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>






        <Custom:ContentControlEx
            Content="{Binding CharacterInfo}" />
        <Custom:ContentControlEx
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1"
            Margin="0,10,0,0"
            Content="{Binding CharacterAbilityScores}" />






    </Grid>
</UserControl>
